const message=document.querySelector('#message');const send=document.querySelector('#send');const clearMessage=document.querySelector('#clearMessage');const clearConversation=document.querySelector('#clearConversation');const screenshots=document.querySelector('#screenshots');const imageCount=document.querySelector('#imageCount');const preview=document.querySelector('#preview');const answer=document.querySelector('#answer');const card=document.querySelector('#answerCard');const contextCard=document.querySelector('#contextCard');const historyEl=document.querySelector('#history');const turnCount=document.querySelector('#turnCount');const status=document.querySelector('#status');const copy=document.querySelector('#copy');let imageData=[];let history=[];try{history=JSON.parse(localStorage.getItem('beStudiosConversation')||'[]')}catch{history=[]}function saveHistory(){localStorage.setItem('beStudiosConversation',JSON.stringify(history))}function renderHistory(){if(!history.length){contextCard.classList.add('hidden');historyEl.innerHTML='';turnCount.textContent='';return}contextCard.classList.remove('hidden');turnCount.textContent=`${history.length} turn${history.length===1?'':'s'} remembered`;historyEl.innerHTML=history.map((item,i)=>`<div class="historyTurn"><strong>${i+1}. Customer</strong><div>${escapeHtml(item.customer||'[from screenshot]')}</div><strong>Be Studios</strong><div>${escapeHtml(item.reply||'')}</div></div>`).join('')}function escapeHtml(value){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function clearImages(){imageData=[];screenshots.value='';preview.innerHTML='';imageCount.textContent='No screenshots added'}screenshots.addEventListener('change',async()=>{const files=[...screenshots.files].slice(0,6);imageData=[];preview.innerHTML='';for(const file of files){if(!file.type.startsWith('image/'))continue;const data=await readFile(file);imageData.push(data);const img=document.createElement('img');img.src=data;img.alt='Conversation screenshot';preview.appendChild(img)}imageCount.textContent=imageData.length?`${imageData.length} screenshot${imageData.length===1?'':'s'} added`:'No screenshots added'});function readFile(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(file)})}send.addEventListener('click',async()=>{const text=message.value.trim();if(!text&&!imageData.length)return;send.disabled=true;send.textContent='Writing…';status.textContent='';try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,images:imageData,history})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Request failed');answer.textContent=data.reply;card.classList.remove('hidden');history.push({customer:text||'[message shown in screenshot]',reply:data.reply});history=history.slice(-8);saveHistory();renderHistory();message.value='';clearImages()}catch(e){status.textContent=e.message}finally{send.disabled=false;send.textContent='Create reply'}});clearMessage.addEventListener('click',()=>{message.value='';answer.textContent='';status.textContent='';card.classList.add('hidden');clearImages();message.focus()});clearConversation.addEventListener('click',()=>{history=[];saveHistory();renderHistory();message.value='';answer.textContent='';status.textContent='';card.classList.add('hidden');clearImages();message.focus()});copy.addEventListener('click',async()=>{await navigator.clipboard.writeText(answer.textContent);copy.textContent='Copied';setTimeout(()=>copy.textContent='Copy',1200)});message.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter')send.click()});renderHistory();