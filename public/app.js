const message=document.querySelector('#message');const send=document.querySelector('#send');const clear=document.querySelector('#clear');const answer=document.querySelector('#answer');const card=document.querySelector('#answerCard');const status=document.querySelector('#status');const copy=document.querySelector('#copy');send.addEventListener('click',async()=>{const text=message.value.trim();if(!text)return;send.disabled=true;send.textContent='Writing…';status.textContent='';try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Request failed');answer.textContent=data.reply;card.classList.remove('hidden')}catch(e){status.textContent=e.message}finally{send.disabled=false;send.textContent='Create reply'}});clear.addEventListener('click',()=>{message.value='';answer.textContent='';status.textContent='';card.classList.add('hidden');message.focus()});copy.addEventListener('click',async()=>{await navigator.clipboard.writeText(answer.textContent);copy.textContent='Copied';setTimeout(()=>copy.textContent='Copy',1200)});message.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter')send.click()});