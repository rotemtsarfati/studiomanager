const message=document.querySelector('#message');const send=document.querySelector('#send');const clearMessage=document.querySelector('#clearMessage');const clearConversation=document.querySelector('#clearConversation');const screenshots=document.querySelector('#screenshots');const imageCount=document.querySelector('#imageCount');const preview=document.querySelector('#preview');const answer=document.querySelector('#answer');const card=document.querySelector('#answerCard');const contextCard=document.querySelector('#contextCard');const historyEl=document.querySelector('#history');const turnCount=document.querySelector('#turnCount');const status=document.querySelector('#status');const copy=document.querySelector('#copy');const feedback=document.querySelector('#feedback');const refine=document.querySelector('#refine');const rememberGuidance=document.querySelector('#rememberGuidance');const guidanceStatus=document.querySelector('#guidanceStatus');let imageData=[];let history=[];let guidance=[];try{history=JSON.parse(localStorage.getItem('beStudiosConversation')||'[]')}catch{history=[]}try{guidance=JSON.parse(localStorage.getItem('beStudiosGuidance')||'[]')}catch{guidance=[]}if(!Array.isArray(guidance))guidance=[];function saveHistory(){localStorage.setItem('beStudiosConversation',JSON.stringify(history))}function saveGuidance(){guidance=guidance.map(x=>String(x||'').trim()).filter(Boolean).slice(-20);localStorage.setItem('beStudiosGuidance',JSON.stringify(guidance));renderGuidanceStatus()}function renderGuidanceStatus(){guidanceStatus.textContent=guidance.length?`${guidance.length} staff preference${guidance.length===1?'':'s'} remembered on this device.`:'Guidance can be remembered on this device for future replies.'}function renderHistory(){if(!history.length){contextCard.classList.add('hidden');historyEl.innerHTML='';turnCount.textContent='';return}contextCard.classList.remove('hidden');turnCount.textContent=`${history.length} turn${history.length===1?'':'s'} remembered`;historyEl.innerHTML=history.map((item,i)=>`<div class="historyTurn"><strong>${i+1}. Customer</strong><div>${escapeHtml(item.customer||'[from screenshot]')}</div><strong>Be Studios</strong><div>${escapeHtml(item.reply||'')}</div></div>`).join('')}function escapeHtml(value){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function clearImages(){imageData=[];screenshots.value='';preview.innerHTML='';imageCount.textContent='No screenshots added'}screenshots.addEventListener('change',async()=>{const files=[...screenshots.files].slice(0,6);imageData=[];preview.innerHTML='';for(const file of files){if(!file.type.startsWith('image/'))continue;const data=await readFile(file);imageData.push(data);const img=document.createElement('img');img.src=data;img.alt='Conversation screenshot';preview.appendChild(img)}imageCount.textContent=imageData.length?`${imageData.length} screenshot${imageData.length===1?'':'s'} added`:'No screenshots added'});function readFile(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(file)})}send.addEventListener('click',async()=>{const text=message.value.trim();if(!text&&!imageData.length)return;send.disabled=true;send.textContent='Writing…';status.textContent='';try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,images:imageData,history,guidance})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Request failed');answer.textContent=data.reply;card.classList.remove('hidden');history.push({customer:text||'[message shown in screenshot]',reply:data.reply});history=history.slice(-8);saveHistory();renderHistory();message.value='';feedback.value='';clearImages()}catch(e){status.textContent=e.message}finally{send.disabled=false;send.textContent='Create reply'}});refine.addEventListener('click',async()=>{const note=feedback.value.trim();const currentReply=answer.textContent.trim();const latest=history[history.length-1];if(!note||!currentReply||!latest)return;refine.disabled=true;refine.textContent='Revising…';status.textContent='';try{const r=await fetch('/api/refine',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:latest.customer==='[message shown in screenshot]'?'':latest.customer,currentReply,feedback:note,history,guidance})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Request failed');answer.textContent=data.reply;latest.reply=data.reply;saveHistory();renderHistory();if(rememberGuidance.checked){const normalized=note.replace(/\s+/g,' ').trim();if(normalized&&!guidance.some(x=>String(x).toLowerCase()===normalized.toLowerCase()))guidance.push(normalized);saveGuidance()}feedback.value=''}catch(e){status.textContent=e.message}finally{refine.disabled=false;refine.textContent='Revise reply'}});clearMessage.addEventListener('click',()=>{message.value='';answer.textContent='';feedback.value='';status.textContent='';card.classList.add('hidden');clearImages();message.focus()});clearConversation.addEventListener('click',()=>{history=[];saveHistory();renderHistory();message.value='';answer.textContent='';feedback.value='';status.textContent='';card.classList.add('hidden');clearImages();message.focus()});copy.addEventListener('click',async()=>{await navigator.clipboard.writeText(answer.textContent);copy.textContent='Copied';setTimeout(()=>copy.textContent='Copy',1200)});message.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter')send.click()});feedback.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter')refine.click()});renderHistory();renderGuidanceStatus();